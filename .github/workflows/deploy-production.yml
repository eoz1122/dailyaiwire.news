name: Deploy Production

on:
  workflow_dispatch:
    inputs:
      ref:
        description: Git ref or commit SHA to deploy
        required: true
        default: main
        type: string
      restart_fetcher:
        description: Restart dailyaiwire_fetcher as part of this deploy
        required: true
        default: false
        type: boolean
      restart_scheduler:
        description: Restart tweet_scheduler as part of this deploy
        required: true
        default: false
        type: boolean

concurrency:
  group: production-deploy
  cancel-in-progress: false

jobs:
  deploy:
    name: Deploy exact ref to production
    runs-on: ubuntu-latest
    environment: production
    permissions:
      contents: read

    steps:
      - name: Checkout requested ref
        uses: actions/checkout@v4
        with:
          ref: ${{ inputs.ref }}
          fetch-depth: 0

      - name: Resolve deploy SHA
        id: meta
        run: echo "sha=$(git rev-parse HEAD)" >> "$GITHUB_OUTPUT"

      - name: Start SSH agent
        uses: webfactory/ssh-agent@v0.9.0
        with:
          ssh-private-key: ${{ secrets.PROD_SSH_KEY }}

      - name: Add production host to known_hosts
        run: ssh-keyscan -H "${{ secrets.PROD_HOST }}" >> ~/.ssh/known_hosts

      - name: Deploy without optional restarts
        if: ${{ inputs.restart_fetcher == false && inputs.restart_scheduler == false }}
        env:
          PROD_HOST: ${{ secrets.PROD_HOST }}
          PROD_USER: ${{ secrets.PROD_USER }}
          DEPLOY_SHA: ${{ steps.meta.outputs.sha }}
        run: |
          ssh "${PROD_USER}@${PROD_HOST}" bash -s -- --ref "${DEPLOY_SHA}" < deploy_to_vps.sh

      - name: Deploy with optional restarts
        if: ${{ inputs.restart_fetcher == true || inputs.restart_scheduler == true }}
        env:
          PROD_HOST: ${{ secrets.PROD_HOST }}
          PROD_USER: ${{ secrets.PROD_USER }}
          DEPLOY_SHA: ${{ steps.meta.outputs.sha }}
          RESTART_FETCHER: ${{ inputs.restart_fetcher }}
          RESTART_SCHEDULER: ${{ inputs.restart_scheduler }}
        run: |
          args=(--ref "${DEPLOY_SHA}")
          if [[ "${RESTART_FETCHER}" == "true" ]]; then
            args+=(--with-fetcher)
          fi
          if [[ "${RESTART_SCHEDULER}" == "true" ]]; then
            args+=(--with-scheduler)
          fi
          ssh "${PROD_USER}@${PROD_HOST}" bash -s -- "${args[@]}" < deploy_to_vps.sh
